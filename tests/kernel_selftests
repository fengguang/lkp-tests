#!/bin/bash
#

cd $BENCHMARK_ROOT/kernel_selftests || exit

for mf in */Makefile; do
	subtest=${mf%/Makefile}
	cmd make run_tests -C $subtest 2> >(tee $RESULT_ROOT/kernel_selftests.info |
		grep -v -e "^info:" \
			-e "^ERROR:" \
			-e "^test:" \
			-e "^make:" >&2)
done
