testcase: tbench
category: benchmark

# upto 40% CPU cycles may be used by latency stats
disable_latency_stats: 1

nr_threads: 100%

perf-profile:

cluster: cs-localhost

if role server:
  tbench-server:

if role client:
  tbench:
