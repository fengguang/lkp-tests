suite: borrow
testcase: borrow

timeout: 1d

sshd:
        pub_key: <%=
        begin
          File.read("#{ENV['HOME']}/.ssh/id_rsa.pub").chomp
        rescue
          nil
        end
        %>
        my_email:

# sleep at the bottom
sleep: 1d
