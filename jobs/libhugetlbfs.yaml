suite: libhugetlbfs
testcase: libhugetlbfs
category: benchmark

libhugetlbfs:
  pagesize:
  - 2MB
  pagenum:
  # Tests need at least 32 hugepages to run.
  # So set the pagenum as minimum and twice of it.
  - 32
  - 64
