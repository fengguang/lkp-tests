suite: suspend
testcase: suspend
category: functional
kernel_cmdline: "initcall_debug log_buf_len=4M no_console_suspend ignore_loglevel"

runtime: 300s
iterations: 10x
presync: presync

suspend:
  mode:
  - mem
  - freeze
