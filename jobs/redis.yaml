suite: redis
testcase: redis
category: benchmark

nr_threads: 100%

cluster: cs-localhost

if role server:
  redis-server:

if role client:
  redis:
    test: set,get
    data_size: 1024
    n_client: 5
    requests: 21000000
    n_pipeline: 3
    key_len: 21000000
