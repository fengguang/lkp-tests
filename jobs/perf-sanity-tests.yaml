suite: perf-sanity-tests
testcase: perf-sanity-tests
category: functional

need_memory: 4G

perf-sanity-tests:
  perf_compiler:
    - gcc
    # disable clang since compiling errors with clang
    # if upstream fix the compiling issue, re-enable it
    # - clang
