suite: power-idle
testcase: power-idle
category: benchmark

# These monitors will not wake up from time to time,
# so it's perfectly OK to include them here.
turbostat:
thermal:
  interval: 30

sleep_after_boot:
  sleep_time: 600

services:
   pmeter_server:

stop_daemons:

runtime: 30m

power-idle:
  idle_time: 600
