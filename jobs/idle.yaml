testcase: idle

default_monitors:
  # These monitors will not wake up from time to time,
  # so it's perfectly OK to include them here.
  latency_stats:
  interrupts:
  softirqs:
  cpuidle:
  turbostat:

stop_daemons:

sleep: 10m
