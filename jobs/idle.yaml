suite: idle
testcase: idle
category: benchmark

# These monitors will not wake up from time to time,
# so it's perfectly OK to include them here.
latency_stats:
interrupts:
softirqs:
cpuidle:
turbostat:

cpufreq_governor:

stop_daemons:

runtime: 10m

sleep:
