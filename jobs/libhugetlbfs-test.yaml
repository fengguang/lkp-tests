suite: libhugetlbfs-test
testcase: libhugetlbfs-test
category: functional

rootfs: debian-x86_64-2016-08-31.cgz

libhugetlbfs-test:
  pagesize:
  - 2MB
  pagenum:
  # Tests need at least 32 hugepages to run.
  # So set the pagenum as minimum and twice of it.
  - 32
  - 64
