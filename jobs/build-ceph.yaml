suite: build-ceph
testcase: build-ceph
category: build

timeout: 6000
need_memory: 8G
testbox: lkp-opteron1

branch: linus/master
commit: RELEASE

ceph_branch: ceph/master
ceph_commit: HEAD

build-ceph:
