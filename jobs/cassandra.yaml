suite: cassandra
testcase: cassandra
category: benchmark

nr_task: 100%

disk: 1SSD

fs: ext4

cluster: cs-localhost

if role server:
  cassandra-server:

if role client:
  cassandra:
    duration: 300s
    insert: 3
    simple1: 1
