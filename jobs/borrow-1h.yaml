suite: borrow
testcase: borrow

sshd:
        pub_key: <%=
        begin
          File.read("#{ENV['HOME']}/.ssh/id_rsa.pub").chomp
        rescue
          nil
        end
        %>
        email:
# sleep at the bottom
sleep: 1h
