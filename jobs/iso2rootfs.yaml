suite: iso2rootfs
category: benchmark
iso2rootfs:
  iso2rootfs_cgz:
  iso_os:
  iso_arch:
  iso_version:
  rootfs_protocol:
  rootfs_server:
  rootfs_path:
  initrd_http_host:
  initrd_http_port:

  # dailybuild related fields
  dailybuild_iso_url_file:

  # submit test yaml related fields
  test_yaml:
  test_os:
  test_os_arch:
  test_os_version:
  test_os_mount:
  test_testbox:
