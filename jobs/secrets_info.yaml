# This job cannot run standalone, it's to be included when submitting other jobs. 

# This file is for user add secret field, so that it's not visible from outside(job.yaml), but can be used at testbox.
# Usage: submit job.yaml -i secrets_info.yaml
# Use at testbox: testbox has env with: "secrets_" + secret field, such as $secrets_my_token

secrets:
  my_token: {{my_token}}
