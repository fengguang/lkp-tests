suite: boot
testcase: boot

boot-slabinfo:
boot-meminfo:
memmap:

kernel_cmdline: rcuperf.shutdown=0

# stats
boot-memory: # depends on memmap and boot-meminfo stats
boot-time:
kernel-size:

kmsg:

timeout: 10m

sleep: 1
