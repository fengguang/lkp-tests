#!/usr/bin/ruby

require 'set'

result = Set.new
while line = STDIN.gets
	case line
	when /^([^ ]+) +(\d+)  (TFAIL|TBROK)  :  /
		result.add $1 + '.' + $2 + '.' + $3
	end
end

result.each { |k| puts "#{k}: 1" }
