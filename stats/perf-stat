#!/usr/bin/env ruby

results = Hash.new(0)

STDIN.each_line do |line|
	_, value, key = line.split
	next unless value =~ /^\d+$/
	results[key] += value.to_i
end

results.each { |key, value|
	puts "#{key}: #{value}"
}
